# SectionDivider

A full-width hairline with an optional numbered mono label. Marks the start of a major section.

```jsx
<SectionDivider index={2} label="experience" />
<SectionDivider />
```

Label and index are optional; with neither it's a plain hairline rule.
