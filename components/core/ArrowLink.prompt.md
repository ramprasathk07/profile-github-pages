# ArrowLink

A text link with a trailing arrow that nudges right on hover — the portfolio's signature inline affordance.

```jsx
<ArrowLink href="#projects">see projects</ArrowLink>
<ArrowLink tone="ink" href="#all">see all repositories on github</ArrowLink>
```

Tones: `accent` (clay, default), `ink`. Underline appears on hover.
